<h1><%= @list.new_record? ? "Create a New List" : "Edit List" %></h1>

<%= simple_form_for @list, html: { multipart: true } do |f| %>
  <%= f.input :name, label: "List Name", input_html: { class: "form-control" } %>
  <%= f.input :photo, as: :file, label: "Upload Image", input_html: { class: "form-control" } %>
  <%= f.button :submit, @list.new_record? ? "Create List" : "Update List", class: "btn btn-primary mt-2" %>
<% end %>

<%= link_to "Back to Lists", lists_path, class: "btn btn-secondary" %>

